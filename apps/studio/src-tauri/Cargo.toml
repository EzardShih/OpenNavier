[package]
name = "opennavier-studio"
version = "0.1.0"
description = "Local-first OpenNavier Studio desktop shell."
authors = ["OpenNavier contributors"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
